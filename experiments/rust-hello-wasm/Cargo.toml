[package]
name = "rust-hello-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
# 优化 WASM 二进制大小
opt-level = "s"          # 优化大小而非速度
lto = true               # Link Time Optimization
codegen-units = 1        # 更好的优化（编译更慢）
